name: webtoon
description: "A Flutter app to explore popular webtoons."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:



  cached_network_image: ^3.0.0

  shared_preferences: ^2.0.0

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

#    - images/ # Assuming you will store your webtoon images in an 'images' directory
#    # Add any specific image assets if needed


#    - images/
